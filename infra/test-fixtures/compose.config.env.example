# Fixture sans secret pour `docker compose config` (tests locaux / CI).
# Copier vers un fichier .env éphémère ou :
#   docker compose -f compose.yml --env-file infra/test-fixtures/compose.config.env.example config db
DB_PASSWORD=compose_config_fixture_only
TRAINING_FORM_TOKEN=compose_config_fixture_only
DASHBOARD_ACCESS_TOKEN=compose_config_fixture_only
