# Variables non secrètes pour les consumers inbox gateway Hermès/Ana.
# Token bridge : LoadCredential systemd (gateway-inbox-bridge-token).

CONTROL_PLANE_URL=http://127.0.0.1:3000
GATEWAY_INBOX_LOOP_MIN_SEC=1
GATEWAY_INBOX_LOOP_MAX_SEC=30
